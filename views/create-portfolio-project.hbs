<h1 class="text-center text-bold">Create portfolio project </h1>
{{#if somethingWentWrong}}
<p>internal error</p>
{{else}}
{{#if validationErrors.length}}
<p>Errors:</p>
<ul>
    {{#each validationErrors}}
    <li>{{this}}</li>
    {{/each}}
</ul>
{{/if}}

<div class="container">
    <div class="columns">
        <div class="column col-2"></div>
        <div class="form-group column col-8">
            <form action="/portfolio/" method="POST">
                <label class="form-label">Title</label>
                <input class="form-input" type="text" name="title" value="{{projectTitle}}" placeholder="enter title ">
                <label class="form-label">content</label>
                <textarea class="form-input" placeholder="enter content" name="content" rows="3">{{projectContent}}</textarea>
                <button class="btn btn-primary" value="create post">submit</button>
            </form>
        </div>
        <div class="column col-2"></div>
    </div>
</div>
{{/if}}